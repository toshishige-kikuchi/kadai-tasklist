<h1>Task一覧</h1>

<ul>
    <% @tasks.each do |task| %>
        <li><%= link_to task.id, task %> : <%= task.content %> &gt; <%= task.status %></li>
    <% end %>
</ul>

<%= paginate @tasks %>

<%= link_to '新しいTaskを追加する', new_task_path %>